# Sound needed here

Add `click.mp3` to this folder. Every clickable element across every page
(subject boxes, table cells, editor buttons, sign-in/out, theme toggle)
plays this file via `js/util.js → playClick()`.

If the file is missing, clicks simply happen silently — nothing breaks.
